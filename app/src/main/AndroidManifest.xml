<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RepairShopManager">
        
        <!-- Login Activity - LAUNCHER -->
        <activity
            android:name=".activities.auth.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Register Activity -->
        <activity
            android:name=".activities.auth.RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />
        
        <!-- Orders Module Activities -->
        <activity
            android:name=".activities.orders.OrdersActivity"
            android:exported="false" />
        
        <activity
            android:name=".activities.orders.CreateOrderActivity"
            android:exported="false" />
        
        <activity
            android:name=".activities.orders.EditOrderActivity"
            android:exported="false" />

        <!-- Stock Module Activities -->
        <activity
            android:name=".activities.stock.CreateStockActivity"
            android:exported="false" />
            
        <activity
            android:name=".activities.stock.EditStockActivity"
            android:exported="false" />

        <!-- Service Module Activities -->
        <activity
            android:name=".activities.service.CreateServiceActivity"
            android:exported="false" />

        <activity
            android:name=".activities.service.EditServiceActivity"
            android:exported="false" />
            
    </application>

</manifest>